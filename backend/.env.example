# Copy to .env and fill in real values locally (never commit .env).

PORT=5000
MONGODB_URI=
JWT_SECRET=

# only needed when running user seed script (src/scripts/seedUsers.js)
SEED_ADMIN_PASSWORD=
SEED_FACULTY_PASSWORD=
SEED_STUDENT_PASSWORD=
